import { FileUploadReservas } from './file-upload-reservas';

describe('FileUploadReservas', () => {
  it('should create an instance', () => {
    expect(new FileUploadReservas()).toBeTruthy();
  });
});
